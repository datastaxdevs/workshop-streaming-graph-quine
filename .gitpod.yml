image:
  file: .gitpod.Dockerfile
tasks:
  - name: setup
    before: |
      curl -L https://github.com/thatdot/quine/releases/download/v1.3.2/quine-1.3.2.jar -o quine-1.3.2.jar
    command: |
      gp open README.md
ports:
  - port: 8080
    onOpen: open-browser
    visibility: public
